//! The crate of solutions.

// Automatically generates all the relevant 'mod' directives for all solution files that exist.
// A solution file is one that is in /src/aoc{year}/day{2-digit number}.rs
autokey::events!("src");

mod util;